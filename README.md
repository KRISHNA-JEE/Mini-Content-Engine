# Mini-Content-Engine
POST /generate - accepts a product name + short description Internally: Call an LLM (several services with small free tiers available) to turn the product info into a good image-generation prompt Generate an image from that prompt and a supporting reference image for the product. I
